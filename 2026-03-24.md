# 2026-03-24 작업 기록

## 09:30 | Q: ek_WbLevel, ek_LectureDe, ek_tdplan 테이블 임포트 + 대시보드에 수업 상세 표시
**질문:** 3개 CSV 테이블을 SQLite로 임포트하고, 학습 현황 대시보드에 수업 단원/소감/조언/메모를 표시하도록 업데이트
**답변:** 3단계로 작업 완료

### STEP 1: CSV 임포트
- `tools/import_lesson_tables.py` 생성 및 실행
- ek_WbLevel: 647행 임포트
- ek_LectureDe: 48,868행 임포트
- ek_tdplan: 112,400행 임포트
- 3-way JOIN 검증: 112,391행 매칭 확인

### STEP 2: 대시보드 쿼리 업데이트
- `app/routers/booking_pages.py` — `dashboard_page` 함수 수정
- 각 수업(lesson)마다 ek_tdplan → ek_WbLevel → ek_LectureDe JOIN으로 상세 정보 조회
- `details` 리스트를 각 lesson dict에 추가 (wb_level_title, student_report, advice, memo)

### STEP 3: 대시보드 템플릿 업데이트
- `templates/booking/dashboard.html` — 멘토 피드백과 별점 사이에 "수업 내용" 섹션 추가
- 각 tdplan 항목을 카드 형태로 표시: 수업 주제, 수강생 소감, 멘토 조언, 수업 메모

---

## 13:00 | Q: 프로토타입 전체화면 HTML 파일 생성
**질문:** kevinsaem-booking의 모든 구현된 화면을 하나의 standalone HTML로 만들어달라. 다른 개발자가 특강멘토용 웹을 만들 때 참고용으로 사용.
**답변:** `케빈샘랩_프로토타입_전체화면.html` 파일 생성 완료 (75KB).
- 총 17개 화면 포함: 수강생 13개 (로그인, 홈, 수업예약, 반복예약, 예약확인, 예약완료, 내 예약, 학습현황, 메시지목록, 채팅, 수강/결제, 공지사항, MY페이지) + 멘토 4개 (멘토홈, 멘토스케줄, 멘토수강생, 멘토메시지)
- Alpine.js로 화면 전환, Tailwind CSS CDN 사용, 동일한 커스텀 컬러(nv, g) 적용
- 상단에 로그인/수강생/멘토 전환 바 포함
- Mock 데이터: 강도열 (취미반 주1회 12주, 제공 12회, 남은 8회), Kevin(C) 멘토, 과거 수업 4건 + 예약 2건
- 서버 없이 파일 더블클릭으로 동작

---

## 14:17 | Q: 프로젝트 종합 점검 (전 페이지 테스트 + 이슈 리포트)
**질문:** kevinsaem-booking 프로젝트의 모든 페이지를 테스트하고, 누락 기능/깨진 링크/일관성 문제를 찾아 보고해달라
**답변:** 테스트 서버(8300 포트)를 띄우고 JWT 토큰 3종(수강생/멘토/관리자)으로 전체 페이지를 테스트했다.

### 1. 페이지 테스트 결과 (전체 200 OK)
- 수강생 11개, 멘토 4개, 관리자 8개, 관리자모바일 3개, 랜딩 1개 = 총 27개 페이지 전부 200 OK
- 깨진 링크: 0개 (252개 href 전수 검사)
- 폼 액션/HTMX URL: 모두 유효한 라우트로 연결

### 2. 발견된 이슈

**BUG (3건)**
1. MY페이지 완료수업 항상 0 — `dev_lesson_history`(0행) 대신 `ek_Sch_Detail_Room_mem`(179행) 사용해야 함
2. 수강생 하단 탭바 active 안됨 — `active_tab` 변수를 라우터에서 전달하지 않음
3. 랜딩 오타 — "멘토이 매 단계마다" → "멘토님이 매 단계마다"

**WARNING (5건)**
4. main.py 타이틀 "케빈샘 AI코딩" → "케빈샘랩" 변경 필요
5. 채팅 teacher_field "AI활용 / Python" 하드코딩
6. MY페이지 출석률 92% 하드코딩
7. 관리자 base 템플릿 favicon 없음
8. 수강생 6개 페이지에 block title 미설정

**INFO (5건)**
9. "강사"는 HTML 주석에만 남아있음 (사용자 미노출)
10. "AI코딩"은 활성 템플릿에 없음 (main.py/requirements.txt에만)
11. "수강/결제" → "결제이력" 변경 완료
12. 카카오 알림톡 API URL이 더미값
13. ADMIN_PASSWORD 설정값 코드에서 미사용

---

## 16:00 | Q: 종합 보안 감사 (Security Audit Final)
**질문:** kevinsaem-booking 프로젝트의 전체 소스코드에 대해 OWASP 기준 보안 감사 수행. A~J 10개 카테고리(인증, SQL Injection, XSS, CSRF, 민감데이터, 입력검증, 비즈니스로직, 결제, API, 인프라) 점검.
**답변:** 17개 파일 전수 분석 완료. `reports/security_audit_final.md`에 상세 보고서 작성.

### 발견 결과 요약
- CRITICAL 5건, MEDIUM 8건, LOW 5건

### CRITICAL (배포 전 반드시 수정)
1. **로그인 Rate Limiting 없음** — 4자리 인증번호 브루트포스 공격 가능 (최대 10,000회 시도)
2. **다른 사람의 settle_code로 예약 가능** — `/booking/complete`에서 form의 settle_code를 그대로 사용
3. **결제 금액으로 패키지 매칭** — 금액 변조 시 저가로 비싼 패키지 구매 가능
4. **연구노트 lecturede_idx 소유권 미검증** — 다른 학생의 연구노트 덮어쓰기 가능
5. **신규가입 비밀번호 MD5(전화번호4자리)** — 예측 가능한 약한 해시

### 안전 확인된 영역
- SQL 파라미터 바인딩 전체 사용 (양호)
- Jinja2 auto-escape 활성화, |safe 미사용 (양호)
- 쿠키 보안 속성 적절 (HttpOnly, Secure, SameSite)
- 역할 기반 접근 제어 정상 동작
- 예약 취소/메시지 발신자 소유권 확인 (양호)
- .gitignore에 민감파일 포함 (양호)

### 생성 파일
- `reports/security_audit_final.md` — 전체 보안 감사 보고서

---

## 17:30 | Q: CRITICAL 보안 이슈 5건 수정
**질문:** 보안 감사에서 발견된 CRITICAL 5건을 코드에서 직접 수정
**답변:** 5건 모두 수정 완료, 앱 정상 로드 확인 (`from main import app` OK)

### FIX 1: 로그인 brute-force 방지 (booking_pages.py)
- `_login_attempts` dict로 IP별 시도 기록
- 15분 내 5회 초과 시 로그인 차단 메시지 반환
- `do_login()` 함수 상단에 rate limiting 로직 추가

### FIX 2: settle_code 도용 방지 (booking_pages.py)
- `/booking/complete`에서 `form.get("settle_code")` 대신 `user["settle_code"]` (JWT) 사용
- 타인의 수강권으로 예약하는 것 원천 차단

### FIX 3: 결제 금액 변조 방지 (payment.py)
- 토스 승인 응답의 `totalAmount`와 패키지 `price` 비교
- 불일치 시 로그 기록 + 수강권 미생성 + 에러 페이지 반환

### FIX 4: 연구노트 소유권 확인 (booking_pages.py)
- `save_research_note()`에서 lecturede_idx가 로그인 유저의 수업인지 JOIN으로 검증
- ek_LectureDe → ek_Lecture → ek_Sch_Detail_Room_mem 소유권 체인 확인
- 소유권 없으면 403 반환

### FIX 5: 신규가입 비밀번호 강화 (auth.py)
- `MD5(phone_last4)` → `MD5(kakao_id + phone_last4)` 변경
- kakao_id는 외부에서 알 수 없으므로 예측 불가

---
