# 2026-03-27 작업 기록

## 10:00 | Q: 로그인 Internal Server Error + 학습 현황 수정
**질문:** 이지원/9398 로그인 시 에러, 학습 현황을 수강권별로 보고 싶음
**답변:**
1. 로그인 에러 수정 — execute_query 대신 _get_mssql_conn 직접 사용, TOP 1 추가
2. datetime 객체 [:10] 슬라이싱 에러 근본 해결 — execute_query에 _convert_row 추가 (datetime→문자열 자동 변환)
3. 학습 현황 전면 수정:
   - ek_Settlement에서 전체 수강권 목록 조회 (최신순)
   - 드롭다운으로 수강권 선택 → 해당 기간 수업 기록 표시
   - 이전 수강권도 조회 가능

---

## 11:30 | Q: 4가지 수정 (mem_MbrType, 소수점, 학습현황 카드, 결제→학습 연동)
**질문:** 회원가입 MbrType 4→2 변경, 결제 금액 소수점 제거, 학습 현황 카드를 4칸 그리드로 개선, 결제 이력에서 학습 현황 바로 이동 링크 추가
**답변:**
1. **mem_MbrType 4→2**: `booking_pages.py` verify에서 `UPDATE mem_MbrType = 2`, `auth.py` 카카오 INSERT에서 `'2'`로 변경
2. **소수점 제거**: `booking_pages.py`의 payment_page에서 `int()` 변환 적용 (current_plan.amount, payment_history.amount)
3. **학습 현황 4칸 그리드**: dashboard_page에서 총 배정(week_tcnt), 예약중(GETDATE() 이후), 완료(GETDATE() 이전), 잔여(총-예약-완료) 쿼리 추가. dashboard.html 요약 카드를 4칸 grid로 변경. 월수강권은 ∞ 표시
4. **결제→학습 연동**: settle_code를 payment_history에 포함, payment.html의 각 결제 항목을 `<a href="/booking/dashboard?sc={settle_code}">`로 감싸서 클릭 시 해당 수강권 학습 현황으로 이동
- 수정 파일: `app/routers/booking_pages.py`, `app/routers/auth.py`, `templates/booking/dashboard.html`, `templates/booking/payment.html`
- Python 문법 검증(ast.parse) 통과

---

---

## 12:30 | Q: 수업 예약 과정 유형 분리 기능 구현
**질문:** 수강권의 class_cnt에 따라 1:1 수업과 그룹 수업을 구분하는 예약 흐름 구현
**답변:** 6개 파일 수정/생성 완료

**수정한 파일:**
1. `app/services/schedule_service.py` — `_get_wb_idx()` → `_get_class_cnt(settle_code)` 변경, `_teacher_filter(class_cnt)` 기반으로 변경 (class_cnt==1→타입4, 그 외→타입5), `_capacity_filter(class_cnt)` 추가 (1:1은 예약1건이면 마감, 그룹은 제한없음), get_available_dates/get_time_slots/get_available_teachers 모두 class_cnt 기반으로 수정
2. `app/services/booking_service.py` — `get_active_settlements(mem_id)` 함수 추가 (ek_Settlement JOIN ek_Package, 잔여>0인 수강권 목록 반환), `create_booking()` 수정 (class_cnt에 따른 정원 체크 분기)
3. `app/routers/booking_pages.py` — `/booking/calendar` 라우트에 settle_code 파라미터 추가 (없으면 수강권 선택 화면, 1개면 자동 통과), HTMX partial 라우트 3개에 settle_code 파라미터 추가, repeat/confirm/complete에 settle_code 전달 흐름 추가
4. `templates/booking/select_settlement.html` — 새로 생성. 활성 수강권 카드 목록 (과정명, 잔여 횟수, 기간, 수업 유형 표시), 클릭시 /booking/calendar?settle_code=XXX
5. `templates/booking/reserve.html` — HTMX 호출에 settle_code 포함, 폼에 settle_code hidden input 추가, JS 변수로 currentSettleCode 전달
6. `static/js/booking.js` — bookingState에 settleCode 추가, HTMX 요청 URL에 settle_code 쿼리 파라미터 포함
7. `templates/booking/repeat.html` — settle_code hidden input 추가

**검증:** ast.parse로 Python 3개 파일 모두 문법 검증 통과


---

## 19:30 | Q: 메시지 시스템 구현 (ek_message 테이블 기반)
**질문:** 케빈샘랩 예약 시스템에 메시지 시스템 구현 — 이메일 발송, 토큰 기반 멘토 답변, 대화 목록/채팅 UI
**답변:** ek_message / ek_message_token 테이블 기반 메시지 시스템을 구현했습니다.

### 새로 생성한 파일
1. `app/services/email_service.py` — Gmail SMTP 이메일 발송 서비스 (send_email, 멘토 알림 이메일 빌더, 수강생 알림 이메일 빌더)
2. `app/routers/message.py` — 메시지 라우터 (prefix="/message")
   - GET /message/ — 메시지함 (대화 목록)
   - GET /message/chat/{mentor_id} — 멘토와의 대화
   - POST /message/send — 메시지 보내기 + 멘토 이메일 알림 (토큰 링크 포함)
   - GET /message/reply?token= — 멘토 답변 페이지 (로그인 불필요)
   - POST /message/reply — 멘토 답변 등록 + 수강생 이메일 알림
3. `templates/message/reply.html` — 멘토 답변 독립 페이지 (수강생 정보, 대화 히스토리, 학습 이력, 답변 작성)

### 수정한 파일
4. `templates/booking/messages.html` — 대화 링크를 /message/chat/로 변경
5. `templates/booking/chat.html` — 뒤로가기 /message/, 전송 폼 /message/send로 변경, mentor_id hidden input 추가
6. `templates/components/tab_nav.html` — 메시지 탭 링크를 /message/로 변경
7. `templates/booking/home.html` — 메시지 바로가기 링크를 /message/로 변경
8. `main.py` — message 라우터 등록
9. `app/routers/booking_pages.py` — 기존 메시지 라우트를 /message/로 301 리다이렉트로 교체

### 핵심 동작
- 수강생이 메시지 보내면 → ek_message에 저장 → ek_message_token 생성 (48시간 유효) → 멘토 이메일로 답변 링크 발송 (BackgroundTasks)
- 멘토가 이메일 링크 클릭 → 로그인 없이 답변 페이지 접근 → 답변 등록 → 수강생 이메일 알림
- 파일 첨부는 TODO로 남김

**검증:** ast.parse로 Python 4개 파일 모두 문법 검증 통과

---

## 22:00 | Q: 홈 공지사항 줄바꿈 + 수업현황 횟수 + 메시지 탭 수정
**질문:** 1) 홈 공지사항 줄바꿈 적용, 2) 수업현황에 제공/예약/남은 횟수 표시, 3) 메시지 탭에 공지가 나오는 오류 수정 + 멘토 메시지 기능 반영

**답변:** 세 가지 수정 완료:

### 1. 홈 공지사항 줄바꿈
- `templates/booking/home.html`: 공지 배너에 `whitespace-pre-line line-clamp-2` 적용
- DB에 저장된 줄바꿈(`\n`)이 화면에 그대로 반영됨

### 2. 수업현황 제공/예약/남은 횟수
- `app/routers/booking_pages.py`: 홈 라우트에 `booked_upcoming` (예약중 수업 수) 계산 추가
- `templates/booking/home.html`: 제공/예약/남은 3개 항목 분리 표시
- 쿼리: `ek_Sch_Detail_Room_mem`에서 현재 시간 이후 status=1인 예약 카운트

### 3. 메시지 탭 수정
**원인:** 
- 홈에서 안읽은 메시지 수를 `dev_messages`(옛 테이블)에서 조회 → `ek_message`로 변경
- dev.db에 `ek_message`, `ek_message_token` 테이블 미생성 → seed.py에 추가 + 기존 DB에도 생성
- 메시지 탭에 기존 대화만 표시, 새 대화 시작 방법 없음

**수정 내용:**
- `app/seed.py`: `ek_message`, `ek_message_token` 테이블 CREATE 추가
- `app/routers/booking_pages.py`: 홈 unread 쿼리를 `ek_message`로 변경
- `app/routers/message.py`: 수강생의 담당 멘토 목록 조회 추가 (수업 이력 기반), 멘토 답변 페이지 학습 이력 쿼리 수정 (`ek_Booking` → `ek_Sch_Detail_Room_mem`)
- `templates/booking/messages.html`: 전체 리디자인 — 대화 목록 + 새 대화 가능 멘토 목록 + 공지 채널 분리 표시

**수정 파일:**
- `templates/booking/home.html`
- `templates/booking/messages.html`
- `app/routers/booking_pages.py`
- `app/routers/message.py`
- `app/seed.py`
- `dev.db` (ek_message, ek_message_token 테이블 직접 생성)
