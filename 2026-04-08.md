# 2026-04-08 작업 기록

## 16:00 | Q: 슈퍼관리자 로그인 기능 테스트 및 보안 점검
**질문:** 슈퍼관리자 비밀번호(SUPER_ADMIN_PASSWORD)로 수강생 계정에 로그인하는 기능 테스트 + 보안 점검
**답변:** pytest 테스트 9개 작성 및 전체 통과, 보안 점검 완료

### 테스트 결과 (9/9 PASS)
- 슈퍼관리자 비밀번호로 기존 회원(김수강) 로그인 성공
- 슈퍼관리자 비밀번호로 다른 회원(이학생, 박공부, 최열심, 정성실) 로그인 성공
- 슈퍼관리자 비밀번호 + 존재하지 않는 이름 -> 로그인 실패
- 일반 인증번호 로그인은 기존대로 동작
- 잘못된 인증번호 -> 로그인 실패 (슈퍼관리자 비번이 설정되어 있어도)
- SUPER_ADMIN_PASSWORD 빈 문자열이면 기능 비활성화
- 빈 문자열 비교를 통한 우회 불가 (bool("" and ...) -> False)
- brute-force 방지 (5회 초과 시 차단)가 슈퍼관리자 로그인에도 적용됨
- 슈퍼관리자 로그인 시 JWT role=student, sub=STU001 올바르게 발급

### 보안 점검 결과
- `.env` 파일: .gitignore에 포함됨 (git에 추적 안됨)
- 코드에 비밀번호 하드코딩: 없음 (SUPER_ADMIN_PASSWORD의 실제 값은 .env에만 존재)
- brute-force 방지: 슈퍼관리자 로그인에도 적용됨 (IP당 15분 내 5회 제한)
- 빈 문자열 우회: `bool("" and code == "")` -> `False`로 안전하게 처리됨

### 보안 경고 사항
1. `ADMIN_PASSWORD: str = "admin1234"` (app/config.py:41) - 기본값이 하드코딩됨. 실서버에서는 반드시 .env로 덮어써야 함
2. `.env.example`에 `SUPER_ADMIN_PASSWORD` 항목 누락 - 추가 권장

### 생성된 파일
- `tests/conftest.py` - pytest 공통 설정
- `tests/test_super_admin_login.py` - 슈퍼관리자 로그인 테스트 9개

---

## 18:30 | Q: kevinsaem_2026(홈페이지) + kevinsaem-booking(예약시스템) 통합
**질문:** 두 개의 FastAPI 프로젝트를 `/Users/kevin/project/kevinsaem_unified/`에 통합
**답변:** 통합 완료. 모든 라우트 200 OK 확인.

### 작업 과정
1. `kevinsaem-booking`을 기반으로 `kevinsaem_unified/` 폴더 생성 (rsync, venv/cache/git/dev.db 제외)
2. `kevinsaem_2026` 홈페이지 템플릿을 `templates/site/`로 복사 (base.html, index.html, youth.html, adult.html, corporate.html, privacy.html, refund.html, components/)
3. 홈페이지 정적파일을 `static/site/`로 복사 (css/, images/)
4. 모든 site/ 템플릿 경로 수정:
   - `extends "base.html"` -> `extends "site/base.html"`
   - `include "components/*"` -> `include "site/components/*"`
   - `/static/css/style.css` -> `/static/site/css/style.css`
   - `/static/images/` -> `/static/site/images/`
5. `app/routers/site_pages.py` 신규 생성 (/, /youth, /adult, /corporate, /privacy, /refund)
6. `main.py` 수정: site_pages 라우터 추가, 기존 `/` 랜딩페이지 라우트 제거

### 해결한 문제
- 포트 충돌: 기존 프로세스가 8000 포트를 점유하고 있어 새 서버 응답이 혼재됨 -> 기존 프로세스 kill 후 해결
- TemplateResponse 호환성: 최신 Starlette에서 `TemplateResponse("template", {"request": request})` 대신 `TemplateResponse(request, "template")` 형태 사용 필요 -> 수정 후 해결

### 테스트 결과 (8/8 PASS)
- `/` -> 200 (홈페이지, CSS/이미지 경로 정상)
- `/youth` -> 200 (청소년 페이지)
- `/adult` -> 200 (성인 페이지)
- `/corporate` -> 200 (기업 페이지)
- `/privacy` -> 200 (개인정보처리방침)
- `/refund` -> 200 (교습비 반환기준)
- `/booking/` -> 200 (예약 시스템)
- `/health` -> 200 (헬스체크)

### 생성/수정된 파일
- `app/routers/site_pages.py` (신규)
- `main.py` (수정: site_pages 라우터 추가, 기존 / 라우트 제거)
- `templates/site/` (신규 폴더: 홈페이지 템플릿 7개 + components 3개)
- `static/site/` (신규 폴더: css/, images/)

---
